function iterativeLog(array){
  array.forEach(function(item, index, array) {
    console.log(`${index}: ${item}`)
  })
};

function iterate(something) {
  const boogie = [0, 1, 2, 3, 4, 5]
  boogie.forEach(something)
  return boogie
}


function doToArray(array, coolbean){
  array.forEach(coolbean)
};
